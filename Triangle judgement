with trianglee as (
    select x,y,z,
    case
        when x>0 and y>0 and z>0 then 
            case 
            when x+y>z and x+z>y and y+z>x then "Yes"
            else "No"
            end 
    else "No"        
    end as triangle   
    from Triangle     
)
select * from trianglee;
